interface Product {
    category: string,
    price: number,
    imageUrl: string
}  
interface Address {
  firstName: string;
  lastName: string;
  streetAddress: string;
  apartmentNumber: string;
  state: string;
  zipcode: number;
}
